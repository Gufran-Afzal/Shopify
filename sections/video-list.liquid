{%- liquid
  assign section_prefix_class = 'video-list'
  assign section_scheme_class = section.settings.section_color_scheme | prepend: 'scheme scheme--'

  capture max_header_size
    echo settings.page_width | times: section.settings.max_header_size | divided_by: 100
  endcapture

  capture container_class
    if section.settings.section_full_width
      echo 'container-fluid'
      else
        echo 'container'
    endif
  endcapture
-%}
{%- style -%}
  .video-list .container-fluid{
    --cp-spacer-left: 0;
    --cp-spacer-right: 0;
  }

  .video-list{
    --focus-outline-offset: 1px;
  }

  .video-list .splide__slide{
    padding: 3px 2px;
  }

  .video-list .video-card .--media-size--blank.--media-size-adapt{
    --ratio-divider: 0.8055;
  }

  .video-list .container-fluid .heading-head{
    margin-bottom: 20px;
    padding-left: 15px;
    padding-right: 15px;
  }

  .video-list_slider-wrapper .video-card{
    filter: grayscale(100%);
    pointer-events: none;
  }

  .video-list_slider-wrapper .splide__slide.is-active .video-card{
    filter: grayscale(0);
    pointer-events: auto;
  }

  .video-list .deferred-media__poster-icn{
    width: 35px;
    height: 35px;
  }

  .video-list .splide-controls{
    margin-top: 18px;
  }

  @media (min-width: 576px){
    .video-list .video-card .--media-size--blank.--media-size-adapt{
      --ratio-divider: 0.7055;
    }

    .video-list .deferred-media__poster-icn{
      width: 50px;
      height: 50px;
    }
  }

  @media (min-width: 768px){
    .video-list .video-card .--media-size--blank.--media-size-adapt{
      --ratio-divider: 0.6055;
    }
  }

  @media (min-width: 992px){
    .video-list .video-card .--media-size--blank.--media-size-adapt{
      --ratio-divider: 0.5055;
    }

    .video-list .heading-head{
      margin-bottom: 35px;
    }

    .video-list .deferred-media__poster-icn{
      width: 70px;
      height: 68px;
    }
  }

  @media (max-width: 575px){
    .video-list .deferred-media__poster-icn svg{
      width: 18px;
      height: 18px;
    }
  }
{%- endstyle -%}
<div class="global-block {{ section_prefix_class }} {{ section_prefix_class }}-{{ section.id }} {{ section_scheme_class }} --align-center" style="--section-padding-top: {{ section.settings.section_top_padding }}px; --section-padding-bottom: {{ section.settings.section_bottom_padding }}px;">
  <div class="{{- container_class -}}">
    <div class="{{ section_prefix_class }}_wrapper">
      {%- if section.settings.section_heading != blank -%}
        <header class="heading-head inner-container --word-break --align-center" style="--max-content: {{ max_header_size }}px;">
          <h2 class="{{ section_prefix_class }}_heading {{ section.settings.section_heading_size }} {{ section.settings.section_heading_weight }} --h-anchor-none mty">
            {{- section.settings.section_heading -}}
          </h2>
        </header>
      {%- endif -%}
      <div class="{{ section_prefix_class }}_slider-wrapper">
        <splide-slider
          data-slider-type="default"
          {% if request.design_mode %}
            data-go-to="true"
          {% endif %}
        >
          <div 
            class="splide"
            data-splide='{
              "drag": false,
              "autoplay": {{- section.settings.auto_play -}},
              "rewind": true,
              "pauseOnHover": true,
              "gap": "30px",
              "perMove": 1,
              "perPage": 1,
              {% if section.blocks.size > 1 %}
                "arrows": {{- section.settings.show_navigation -}},
                "pagination": {{- section.settings.show_pagination -}},
              {% else %}
                "arrows": false,
                "pagination": false,
              {% endif %}
              "interval": {{- section.settings.change_slides_every | times: 1000 -}},
              {% if section.settings.section_full_width %}
                "padding": "5%",
                "mediaQuery": "min",
                  "breakpoints": {
                    "576": {
                      "padding": "20%"
                    }
                  },
              {% endif %}
               "focus": "center",
              {% if request.design_mode == false %}
                "type": "loop"
              {% else %}
                "type": "slide"
              {% endif %}
            }'
          >
            <div class="splide__track">
              <div class="splide__list splide-slider--h-scroll" style="--init-divider: 1; --init-mob-divider: 1;">
                {%- if section.blocks.size > 0 -%}
                  {%- for block in section.blocks -%}
                    {%- liquid
                      capture loading_attr
                        unless section.index != 1
                          unless forloop.index != 1
                            echo 'eager'
                            else
                              echo 'lazy'
                          endunless
                          else
                            echo 'lazy'
                        endunless
                      endcapture
                    -%}
                    <div class="splide__slide" {{ block.shopify_attributes }}>
                      {%- render 'video-card', card: block, prefix_class: section_prefix_class, index: forloop.index, card_media_option: section.settings.section_media_size, widths_attr: '286, 380, 606, 734, 958, 1196, 1555', sizes_attr: '(max-width: 1259px) calc(100vw - 30px), calc(var(--cp-width-px) - 34px)', width_attr: 1555, loading_attr: loading_attr -%}
                    </div>
                  {%- endfor -%}
                {%- endif -%}
              </div>
            </div>
            {%- if section.blocks.size > 1 -%}
              {%- if section.settings.show_navigation or section.settings.show_pagination -%}
                <div class="splide-controls splide__arrows --justify-center">
                  {%- if section.settings.show_navigation -%}
                    <button class="splide__arrow splide__arrow--prev button button--primary button--icn">
                      <span class="visually-hidden">{{- 'accessibility.slider.previous' | t -}}</span>
                      {%- render 'icons-data', width: 10, height: 10, use_icon: true, icon_reference: 'left-arrow' -%}
                    </button>
                  {%- endif -%}
                  <ul
                    class="splide__pagination"
                    role="tablist"
                    aria-label="{{- 'accessibility.slider.pagination_label' | t -}}"
                  ></ul>
                  {%- if section.settings.show_navigation -%}
                    <button class="splide__arrow splide__arrow--next button button--primary button--icn">
                      <span class="visually-hidden">{{- 'accessibility.slider.next' | t -}}</span>
                      {%- render 'icons-data', width: 10, height: 10, use_icon: true, icon_reference: 'right-arrow' -%}
                    </button>
                  {%- endif -%}
                </div>
              {%- endif -%}
            {%- endif -%}
          </div>
        </splide-slider>
      </div>
    </div>
  </div>
</div>
{% schema %}
{
  "name": "t:sections.video_list.name",
  "tag": "section",
  "disabled_on": {
    "groups": [
      "header",
      "footer",
      "custom.overlay"
    ]
  },
  "settings": [
    {
      "type": "header",
      "content": "t:sections.head.content"
    },
    {
      "type": "inline_richtext",
      "id": "section_heading",
      "label": "t:heading.label"
    },
    {
      "type": "select",
      "id": "section_heading_size",
      "label": "t:sizes.heading.label",
      "options": [
        {
          "value": "h1",
          "label": "t:sizes.heading.options.opt_1.label"
        },
        {
          "value": "h2",
          "label": "t:sizes.heading.options.opt_2.label"
        },
        {
          "value": "h3",
          "label": "t:sizes.heading.options.opt_3.label"
        },
        {
          "value": "h4",
          "label": "t:sizes.heading.options.opt_4.label"
        }
      ],
      "default": "h3"
    },
    {
      "type": "select",
      "id": "section_heading_weight",
      "label": "t:weight.heading.label",
      "options": [
        {
          "value": "--fw-bold",
          "label": "t:weight.heading.options.opt_1.label"
        },
        {
          "value": "--fw-semi-bold",
          "label": "t:weight.heading.options.opt_5.label"
        },
        {
          "value": "--fw-medium",
          "label": "t:weight.heading.options.opt_2.label"
        },
        {
          "value": "--fw-normal",
          "label": "t:weight.heading.options.opt_3.label"
        }
      ],
      "default": "--fw-bold"
    },
    {
      "type": "range",
      "id": "max_header_size",
      "label": "t:sections.head.size.label",
      "min": 50,
      "max": 100,
      "step": 5,
      "default": 100,
      "unit": "%"
    },
    {
      "type": "header",
      "content": "t:layout.content"
    },
    {
      "type": "select",
      "id": "section_media_size",
      "label": "t:media.label",
      "options": [
        {
          "value": "adapt",
          "label": "t:media.options.opt_1.label"
        },
        {
          "value": "square",
          "label": "t:media.options.opt_2.label"
        },
        {
          "value": "portrait",
          "label": "t:media.options.opt_3.label"
        },
        {
          "value": "landscape",
          "label": "t:media.options.opt_4.label"
        }
      ],
      "default": "landscape"
    },
    {
      "type": "checkbox",
      "id": "section_full_width",
      "label": "t:sections.full_width.label",
      "default": false
    },
    {
      "type": "color_scheme",
      "id": "section_color_scheme",
      "default": "scheme1",
      "label": "t:color_scheme.label"
    },
    {
      "type": "header",
      "content": "t:slideshow.content"
    },
    {
      "type": "checkbox",
      "id": "auto_play",
      "label": "t:slideshow.auto_play.label",
      "default": false
    },
    {
      "type": "checkbox",
      "id": "show_navigation",
      "label": "t:slideshow.show_arrows.label",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_pagination",
      "label": "t:slideshow.show_nav.label",
      "default": true
    },
    {
      "type": "range",
      "id": "change_slides_every",
      "label": "t:slideshow.interval.label",
      "min": 2,
      "max": 9,
      "step": 1,
      "default": 5,
      "unit": "s",
      "info": "t:slideshow.interval.info"
    },
    {
      "type": "header",
      "content": "t:spacing.content"
    },
    {
      "type": "range",
      "id": "section_top_padding",
      "label": "t:spacing.settings.top.label",
      "min": 0,
      "max": 100,
      "step": 1,
      "default": 50,
      "unit": "px"
    },
    {
      "type": "range",
      "id": "section_bottom_padding",
      "label": "t:spacing.settings.bottom.label",
      "min": 0,
      "max": 100,
      "step": 1,
      "default": 50,
      "unit": "px"
    }
  ],
  "blocks": [
    {
      "name": "t:video.name",
      "type": "video",
      "limit": 10,
      "settings": [
        {
          "type": "image_picker",
          "id": "cover_image",
          "label": "t:video.cover.label",
          "info": "t:sections.video_list.settings.image.info"
        },
        {
          "type": "text",
          "id": "alt_text",
          "label": "t:video.alt.label",
          "info": "t:sections.video_list.settings.alt_text.info"
        },
        {
          "type": "checkbox",
          "id": "enable_video_looping",
          "default": false,
          "label": "t:video.loop.label"
        },
        {
          "type": "header",
          "content": "t:video.header.label"
        },
        {
          "type": "video",
          "id": "upload_video",
          "label": "t:video.label"
        },
        {
          "type": "header",
          "content": "t:video.header-embed.label",
          "info": "t:video.header-embed.info"
        },
        {
          "type": "video_url",
          "id": "video_url",
          "label": "t:video.url.label",
          "accept": [
            "youtube",
            "vimeo"
          ],
          "default": "https://www.youtube.com/watch?v=_9VUPq3SxOc"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "t:sections.video_list.name",
      "blocks": [
        {
          "type": "video",
          "settings": {
          }
        },
        {
          "type": "video",
          "settings": {
          }
        },
        {
          "type": "video",
          "settings": {
          }
        }
      ]
    }
  ]
}
{% endschema %}